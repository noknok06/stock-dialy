# 改善対応方針（2026-06 決定）

「株式の分析・記録・想起ツール」としての評価をもとに、機能の過不足を1論点ずつ確認して決定した対応方針。
プロダクトの判断軸は `docs/product_context.md` に従う。

---

## 決定事項一覧

| # | 論点 | 決定 | 状態 |
|---|------|------|------|
| 1 | CopoMo（earnings_analysis）の扱い | **おまけツールとして存置** | ✅ 実装済み |
| 2 | EDINET開示の想起通知 | **設計確定＆実装**（Stage 1-2 稼働中） | ✅ 実装済み |
| 3 | 売却完結時の振り返り | **控えめに実装**（バッジ+記入ボタン） | ✅ 実装済み |
| 4 | インポート後の理由未記入キュー | **見送り** | ❌ 見送り |
| 5 | タグ別・テーマ別の成績集計 | **trading_dashboard に追加** | ✅ 実装済み |
| 6 | 全銘柄横断タイムライン | **専用ページとして新設** | ✅ 実装済み |
| 7 | バックリンク表示 | **detail の関連タブに統合** | ✅ 実装済み |
| 8 | データエクスポート | **CSVのみ実装** | ✅ 実装済み |
| 9 | 記入欄の整理 | **DiaryNote は Markdown 化済み / memo は部分対応** | ⚠️ 部分実装 |
| 10 | ナビ・ページ構成 | **メニューを主要5項目に縮退** | ✅ 実装済み |

---

## 部分実装・未完了

### 9. 記入欄の整理（部分実装）

- `DiaryNote.content` の Markdown 化: ✅ 表示は既存の `render_markdown` 基盤を流用、入力欄も軽量エディタ化
- `memo` 廃止: ⚠️ フォーム・表示コメントでは廃止宣言だが、DB フィールド（StockDiary.memo）は残存
  - **未完了**: 既存データの reason への統合マイグレーション未実施
  - **対応**: マイグレーションを作成し、memo → reason へのデータ移行を完了する

## 追加決定: 再評価に基づく引き算と初回体験（2026-06・部分実装）

主要機能実装後の再評価で「足し算の負債」と「新規体験の穴」を確認。

**引き算（情報密度の回収）** ✅ 実装済み
- 想起カードのモバイル折りたたみ実装
- detail タブ併存は検証済み（統合撤回）
- PCサイドバー・tab_notes.html の削除撤回

**初回体験（最初の5分）** ⚠️ 部分実装
- 登録後の自動ログイン: 状態不明
- ホーム空状態時の3ステップオンボーディングカード: 実装度確認が必要
- タイムライン・ダッシュボードの空状態リンク追加: 状態不明

## 見送り・対象外

| 項目 | 判断 | 理由 |
|------|------|------|
| 論点4: インポート後の理由未記入キュー | 見送り | 取引一括取込方針に基づく（都度の理由記入は非現実的） |
| Markdown 一括エクスポート | 見送り | CSV のみで十分 |
| investment_hub の廃止・縮退 | 見送り | 現状維持 |
